GitHub Actions → Railway (token PROJET obligatoire)
==================================================

ERREUR "Invalid" ou "Unauthorized" = mauvais token ou mauvais endroit.

❌ NE PAS utiliser : Avatar → Account Settings → Tokens

✅ Token PROJET (à recréer si doute) :
   1. Projet reasonable-healing → Settings → Tokens
   2. Supprime l'ancien token si tu peux
   3. Create Token → environnement "production" (même env que rugtracker)
   4. Copie tout de suite dans GitHub (les anciens tokens ne marchent plus après regen)

Note : "railway whoami" échoue souvent avec un token projet — c'est normal.
Le workflow n'utilise plus whoami.

   Si tu ne vois pas Tokens :
   - Service rugtracker → Settings → cherche "Project Token" / "Generate Token"
   - Ou : Cmd+K sur Railway → cherche "token"

GitHub :
   Repo rugtracker → Settings → Secrets and variables → Actions
   Secret name : RAILWAY_TOKEN
   Value : colle le token PROJET (sans guillemets, sans espace avant/après)

Push :
   git add -A && git commit -m "ci: fix railway project token" && git push origin main

IMPORTANT — brancher GitHub sur Railway (sinon redeploy = ancien code) :
   Service rugtracker → Settings → Source → Connect GitHub
   Repo : mileydelatreche-43Y4EH/rugtracker · branche main

Le workflow utilise "railway redeploy" (pas railway up) car upload = timeout.

IDs utiles (depuis tes logs) :
   Service ID : 94e5fd30-60f9-4b22-8b43-46d5b931ad0a
   Project ID : 8b8f1a76-e839-4560-9f3bd70c352bf

Vérifier :
   GitHub → Actions → Deploy Railway → vert
   Railway → Deployments → nouveau build (commit GitHub visible)
